============
Installation
============

The preferred way to install ``python-namecheap``::

    $ pip install python-namecheap

In your project, you should add ``python-namecheap`` to your ``requirements.txt``.

Be sure to use ``virtualenv`` if you develop python projects.


After installation, the :doc:`quickstart` will get you on your way to using ``python-namecheap``.